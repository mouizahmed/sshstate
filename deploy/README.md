# deploy

Container and service-manager assets.

- `Dockerfile` / compose for the sync relay — Milestone 2 (one non-root
  container, persistent volume, configurable HTTPS reverse proxy, no telemetry).
- launchd `.plist` and systemd user `.socket`/`.service` units for socket
  activation — Milestone 1 (launchd) and Milestone 3a (systemd).

Empty at Milestone 0.
